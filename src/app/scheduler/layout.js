// import Layout from "@/layouts"

export const metadata = {
  title: 'Sitback - Scheduler',
  description: 'Sitback and relax.',
}

export default function SchedulerLayout({ children }) {
  return (
    <>
      <>{children}</>
    </>
  )
}
